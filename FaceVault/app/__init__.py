"""FaceVault — offline photo management with local face recognition.

All AI inference runs on-device via OpenCV's YuNet (detection) and
SFace (embeddings). No network access is required at runtime.
"""

__version__ = "0.1.0"
