# Core (headless CLI works with just these)
opencv-python-headless>=4.8
numpy>=1.24
Pillow>=10.0
SQLAlchemy>=2.0

# Desktop GUI (optional — install to use `python -m app gui`)
PySide6>=6.6

# Semantic search (optional — natural-language photo search via local CLIP;
# also run models/download_models.py once to fetch the CLIP models)
onnxruntime>=1.17
tokenizers>=0.15
# GPU: replace onnxruntime with onnxruntime-directml (Windows, any GPU)
# or onnxruntime-gpu (NVIDIA CUDA). Verify with: python -m app gpu

# OCR (optional — search photos by the text inside them)
rapidocr-onnxruntime>=1.3

# Development
pytest>=7.0
